spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:mem:blog
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always

engine: h2
